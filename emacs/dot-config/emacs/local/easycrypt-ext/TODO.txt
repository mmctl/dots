- Functionality:
  - ece docgen project dwim: Given the project's root directory, recursively generate documentation for each subdirectory, placing the resulting documentation files in
  the directories of the corresponding source files (or a separate docs/ directory in the
  directory of these source files)
- relative file/direcotry validation for defcustoms

- Templates:
   - async while?
   - code positions?
   - indicate optional fields in info templates?
   - indicate potential for side in info templates?
   - rndsem
   - outline
  
