- Functionality:
- Hideshow integration: Have user option to enable hideshow and automatically
hide everything non-local, or everything non-local?
- Use PTY with runtest, not Pipe (as process connection type)

- Templates:
   - async while?
   - code positions?
   - indicate optional fields in info templates?
   - indicate potential for side in info templates?
   - rndsem
   - outline
  
